import{FormEvent,useEffect,useState}from'react';import{api}from'../api/client';import{useNavigate}from'react-router-dom';export default function NewTicket(){const[m,setM]=useState<any>();const nav=useNavigate();const[f,setF]=useState<any>({title:'',description:'',priority:1,customerId:'',categoryId:'',assignedUserId:''});useEffect(()=>{api('/meta').then(setM)},[]);async function submit(e:FormEvent){e.preventDefault();const r=await api('/tickets',{method:'POST',body:JSON.stringify({...f,customerId:+f.customerId,categoryId:f.categoryId?+f.categoryId:null,assignedUserId:f.assignedUserId?+f.assignedUserId:null,priority:+f.priority})});nav('/tickets/'+r.id)}if(!m)return <>Loading…</>;return <><div className="page-head"><div><h1>Create ticket</h1><p>Capture the issue and route it to the right person.</p></div></div><form className="panel form" onSubmit={submit}><label>Title<input required value={f.title} onChange={e=>setF({...f,title:e.target.value})}/></label><label>Description<textarea required rows={7} value={f.description} onChange={e=>setF({...f,description:e.target.value})}/></label><div className="form-row"><label>Customer<select required value={f.customerId} onChange={e=>setF({...f,customerId:e.target.value})}><option value="">Choose customer</option>{m.customers.map((x:any)=><option value={x.id}>{x.name}</option>)}</select></label><label>Priority<select value={f.priority} onChange={e=>setF({...f,priority:e.target.value})}><option value="0">Low</option><option value="1">Normal</option><option value="2">High</option><option value="3">Critical</option></select></label></div><div className="form-row"><label>Category<select value={f.categoryId} onChange={e=>setF({...f,categoryId:e.target.value})}><option value="">No category</option>{m.categories.map((x:any)=><option value={x.id}>{x.name}</option>)}</select></label><label>Assignee<select value={f.assignedUserId} onChange={e=>setF({...f,assignedUserId:e.target.value})}><option value="">Unassigned</option>{m.users.map((x:any)=><option value={x.id}>{x.name}</option>)}</select></label></div><div><button className="primary">Create ticket</button></div></form></>}