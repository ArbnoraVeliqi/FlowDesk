import{FormEvent,useState}from'react';import{api}from'../api/client';export default function Login(){const[email,setEmail]=useState('admin@flowdesk.dev');const[password,setPassword]=useState('Demo123!');const[error,setError]=useState('');async function submit(e:FormEvent){e.preventDefault();try{const r=await api('/auth/login',{method:'POST',body:JSON.stringify({email,password})});localStorage.setItem('token',r.token);localStorage.setItem('user',JSON.stringify(r.user));location.href='/';}catch(x:any){setError(x.message)}}return <div className="login"><div className="login-copy"><div className="brand light"><span>F</span>FlowDesk</div><h1>Support work,<br/>without the noise.</h1><p>One place for customer conversations, ownership and the work that needs attention.</p></div><form onSubmit={submit} className="login-card"><h2>Welcome back</h2><p>Sign in to your workspace.</p>{error&&<div className="error">{error}</div>}<label>Email<input value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label><button className="primary">Sign in</button><small>Demo: admin@flowdesk.dev / Demo123!</small></form></div>}